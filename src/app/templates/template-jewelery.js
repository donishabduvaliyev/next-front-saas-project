export default function JewelryTemplate({shop}) {
    return (

        <div>
            <h1>Jewelry Template for {shop.name}</h1>
            <p>This is a template for displaying jewelry items.</p>
        </div>
    );
}