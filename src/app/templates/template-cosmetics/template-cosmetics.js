export default function CosmeticsTemplate({shop}) {
    return (

        <div>
            <h1>Cosmetics Template for {shop.name}</h1>
            <p>This is a template for displaying jewelry items.</p>
        </div>
    );
}