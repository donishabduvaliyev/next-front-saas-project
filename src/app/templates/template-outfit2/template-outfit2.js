export default function Outfit2Template({shop}) {
    return (

        <div>
            <h1>Outfit2 Template for {shop.name}</h1>
            <p>This is a template for displaying jewelry items.</p>
        </div>
    );
}