export default function WatchTemplate({shop}) {
    return (

        <div>
            <h1>watch Template for {shop.name}</h1>
            <p>This is a template for displaying jewelry items.</p>
        </div>
    );
}