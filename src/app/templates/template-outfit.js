export default function OutfitTemplate({shop}) {
    return (

        <div>
            <h1>Outfit Template for {shop.name}</h1>
            <p>This is a template for displaying jewelry items.</p>
        </div>
    );
}